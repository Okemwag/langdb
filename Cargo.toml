[package]
name = "langdb"
version = "0.1.0"
edition = "2021"

[workspace]
members = ["web_app"]

[dependencies]
nom = "7.1.3"
thiserror = "1.0.69"
anyhow = "1.0.95"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.140"
chrono = "0.4.39"

# Web dependencies (optional if feature flagged, but keeping global for simplicity if allowed)
# Actually web dependencies should be in web_app/Cargo.toml mainly, except for langdb's use in web_app.
# I'll keep common deps here.
